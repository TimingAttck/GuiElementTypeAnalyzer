package GuiElementTypeInferring;

/**
 * The GuiElementInferredDataType Enum
 * denotes different types of data that a Gui Element
 * may hold, that may be of interest during static analysis.
 */
public enum GuiElementInferredDataType {

    PHONE_NUMBER,
    NUMERIC,
    PASSWORD,
    DATE,
    CREDIT_CARD,
    EMAIL_ADDRESS,
    URL,
    IBAN,
    IP_ADDRESS,
    STRING,
    HOLDS_NO_DATA,
    URI,
    ADDRESS

}
